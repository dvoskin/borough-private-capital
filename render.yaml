services:
  - type: web
    runtime: static
    name: borough-private-capital
    buildCommand: "echo 'Using the committed static site build'"
    staticPublishPath: render-dist
    autoDeploy: true
    previews:
      generation: automatic
