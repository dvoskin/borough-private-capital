services:
  - type: web
    runtime: static
    name: borough-private-capital
    plan: free
    buildCommand: "echo 'Using the committed optimized static build'"
    staticPublishPath: render-dist
    autoDeploy: true
    previews:
      generation: automatic
